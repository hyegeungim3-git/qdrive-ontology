/**
 * 문법이 발행으로 바꿀 수 있는 값들.
 *
 * shacl.ts(규칙을 쓰는 쪽)와 grammar.ts(규칙을 고치는 쪽)가 둘 다 필요로 해서,
 * 어느 쪽에 두어도 순환 의존이 된다. 그래서 아무것도 import하지 않는 모듈로 뺐다.
 */

/**
 * 회차 연비의 상식 범위 (m³/km). 시뮬레이터 실측 회차는 0.54~0.63에 모인다.
 * 상한을 1.0으로 두면 정상 회차는 전부 통과하고, 누적값이 섞이면 바로 벗어난다.
 * 문법 개정(임계값 재산정)으로 바뀔 수 있으므로 상수가 아니라 가변 홀더다.
 */
export const FUEL_LIMIT = { min: 0.3, max: 1.0 }

/** 메시지에 «1»이 아니라 «1.0»으로 보이게 — 임계값은 자릿수까지 규격이다 */
export const perKm = (n: number) => n.toFixed(1)

/** 문법 개정으로 꺼진 도메인 규칙. shacl.ts가 이 집합을 보고 뺀다. */
export const DISABLED_RULES = new Set<string>()
